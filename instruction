### Setup instructions
## (must install mongodb first.)

## Install all modules
$ npm install

## Start a mongo server
$ mkdir mongo-data
$ mongod --dbpath ./mongo-data
$ mongod --fork

## Start web server
$ node server.js

